Deploy MCP Hub from https://github.com/online111111/mcp-hub for me.

First inspect the target environment and any existing installation. Decide whether this is a server/VPS deployment, local Hub, native HTTP client connection, or stdio bridge. Prefer the latest stable GitHub Release and verify SHA256SUMS; build from source only when necessary. Never expose port 8080 publicly. Public hosting must keep the Hub on loopback behind HTTPS and use distinct MCP/Admin tokens of at least 32 characters. Keep secrets out of command arguments and final output. Back up existing binary/config/service/proxy files before changes, run `mcp-hub validate` before restart, and verify with `status` + `doctor` after deployment. If the target client is stdio-only, install the local binary and bridge with `mcp-hub stdio --connect <https://host/mcp>`; otherwise prefer native Streamable HTTP. Preserve existing downstream configuration unless I explicitly ask to replace it. If a change breaks a previously healthy deployment, roll back to the last known-good state and report the cause.

Complete the work directly when you have shell/SSH access. If you do not, give me exact OS-specific commands and files instead of claiming deployment succeeded. At the end, report version, binary/config/service paths, MCP/Admin endpoints, health results, HTTPS state, and backup location, but never print token values.
