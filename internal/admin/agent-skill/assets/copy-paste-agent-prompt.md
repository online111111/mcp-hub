Deploy MCP Manager for me. The project is being renamed from `online111111/mcp-manager` to `online111111/mcp-manager`; use the new repository name when it exists, and treat the old repository/install name as a migration source rather than a separate product.

First inspect the target environment and any existing MCP Hub/MCP Manager installation. Decide whether this is a server/VPS deployment, local Manager, native HTTP client connection, Remote Admin client, or stdio bridge. Prefer a stable GitHub Release and verify `SHA256SUMS`; build from source only when necessary. Never expose port 8080 publicly. Public hosting must keep MCP Manager on loopback behind HTTPS and use distinct MCP/Admin tokens of at least 32 characters. New deployments use `MCP_MANAGER_TOKEN` and `MCP_MANAGER_ADMIN_TOKEN`; preserve legacy `MCP_HUB_TOKEN` and `MCP_HUB_ADMIN_TOKEN` when upgrading an existing deployment unless rotation is explicitly requested.

Back up existing binary/config/service/proxy files before changes. Run `mcp-manager validate` before restart and verify with `mcp-manager status` plus `mcp-manager doctor` afterward. Prefer `mcp-manager admin` for authenticated remote downstream management. For stdio-only clients, use `mcp-manager stdio --connect <https://host/mcp>`; otherwise prefer native Streamable HTTP. Preserve existing downstream configuration unless I explicitly ask to replace it. If a change breaks a previously healthy deployment, roll back to the last known-good state and report the cause.

Complete the work directly when you have shell/SSH access. If you do not, give exact OS-specific commands and files instead of claiming deployment succeeded. At the end, report version, binary/config/service paths, MCP/Admin endpoints, health results, HTTPS state, and backup location, but never print token values.
